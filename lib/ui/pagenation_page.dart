import 'package:flutter/material.dart';

class PageNationPage extends StatelessWidget {
  const PageNationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PageNation"),),
    );
  }
}
